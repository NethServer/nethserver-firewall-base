#
# 30extras
#
{
    use esmith::NetworksDB;
    my $ndb = esmith::NetworksDB->open_ro();

    if ($ndb->blue) {
        $OUT .="blue     ipv4\n";
    }
    if ($ndb->orange) {
        $OUT .="orang     ipv4\n"; #truncate zone name to 5 chars
    }
}

